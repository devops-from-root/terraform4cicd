# terraform4cicd

```bash
docker run --rm ghcr.io/devops-from-root/terraform4cicd:latest sh -lc 'terraform version && echo node version && node -v && echo npm version && npm -v && git --version && unzip -v | head -n 1 && bash --version | head -n 1'
```

Original terraform image extended with node.js, npm, git, unzip & bash

Оригинальный образ terraform с добавлением with node.js, npm, git, unzip и bash
