# terraform4cicd

```bash
docker run --rm ghcr.io/devops-from-root/terraform4cicd:latest terraform version
```

Original terraform image extended with node.js, npm, git, unzip & bash

Оригинальный образ terraform с добавлением with node.js, npm, git, unzip и bash
